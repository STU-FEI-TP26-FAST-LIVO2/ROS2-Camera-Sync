cd ~/ros2_tim_projekt
source /opt/ros/humble/setup.bash
source install/setup.bash
./Lidar_NO_STM.sh

Ak neprídu dáta, pošli mi hneď výstup z:

ros2 node list --no-daemon
ros2 topic info /hesai/pandar --verbose
ros2 topic echo /hesai/pandar --once

